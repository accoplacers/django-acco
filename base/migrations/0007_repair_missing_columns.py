# Generated by Antigravity to fix missing columns issue

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('base', '0006_fix_job_opening_schema'),
    ]

    operations = [
        migrations.SeparateDatabaseAndState(
            database_operations=[
                # Registration.password already exists (based on error), so we skip it.
                # Registration.email update is also likely done if password is there, skipping to be safe.
                
                # From 0004 (skipped by fake, confirmed missing)
                migrations.AddField(
                    model_name='registration',
                    name='photo',
                    field=models.ImageField(blank=True, null=True, upload_to='employee_photos/'),
                ),
                
                # From 0005 (skipped by fake, likely missing since it's after 0004)
                migrations.AddField(
                    model_name='registration',
                    name='skills',
                    field=models.TextField(blank=True, help_text='Comma-separated list of skills (e.g., Excel, Tally, SAP, QuickBooks)', null=True),
                ),
            ],
            state_operations=[
                # State already thinks these exist because 0003, 0004, 0005 were faked.
            ]
        ),
    ]
